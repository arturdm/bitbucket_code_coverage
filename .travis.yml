language: dart
sudo: false
dart:
- stable
- dev

cache:
  directories:
  - $HOME/.pub-cache
  - .dart_tool/build

stages:
- analyze
- test

jobs:
  include:
  - stage: analyze
    script:
    - dartanalyzer --fatal-warnings .
  - stage: test
    script:
    - pub run test_coverage
    - bash <(curl -s https://codecov.io/bash)
